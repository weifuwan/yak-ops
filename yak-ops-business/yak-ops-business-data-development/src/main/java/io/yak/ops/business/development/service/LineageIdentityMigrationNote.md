# Lineage identity migration

Phase 1 migrates lineage assets toward resolved physical identities.

Goals:

- avoid same-name table collisions across schemas
- centralize asset key generation
- prepare SQL and Dataset lineage to share identity resolution
